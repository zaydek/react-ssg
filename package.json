{
	"name": "react-ssg",
	"license": "MIT",
	"scripts": {
		"force-refresh": "osascript -e 'tell application \"Google Chrome\" to reload active tab of window 1'",
		"prerender": "ts-node --transpile-only src/server/ssg.tsx",
		"build-dev": "NODE_ENV=development yarn prerender && node esbuild.js && yarn force-refresh",
		"build-prod": "NODE_ENV=production yarn prerender && node esbuild.js && yarn force-refresh",
		"watch-dev": "yarn watch --interval 0.1 'yarn build-dev' src",
		"watch-prod": "yarn watch --interval 0.1 'yarn build-prod' src",
		"serve": "yarn http-server dist --brotli"
	},
	"dependencies": {
		"react": "^17.0.1",
		"react-dom": "^17.0.1",
		"react-router-dom": "^5.2.0"
	},
	"devDependencies": {
		"@types/node": "^14.14.10",
		"@types/react": "^17.0.0",
		"@types/react-dom": "^17.0.0",
		"@types/react-router-dom": "^5.1.6",
		"esbuild": "^0.8.18",
		"http-server": "^0.12.3",
		"ts-node": "^9.1.0",
		"typescript": "^4.1.2",
		"watch": "^1.0.2"
	}
}
